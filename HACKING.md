# developing revo

this doc describes the details behind revo's development, for contribution guidelines see [./CONTRIBUTING.md](./CONTRIBUTING.md)

## tooling

### zlint

the zig linter, config is at `zlint.json`

```sh
zlint
zlint --fix # sometimes you want this
```

### debugger/profiler

you wanna use lldb for zig. the debug build is unoptimized and keeps symbols

```sh
zig build
lldb ./zig-out/bin/revo
(lldb) run -e "spawn fn() add(20, 22)"
```

rss on macos: `/usr/bin/time -l ./zig-out/bin/revo bench/skynet.rv 2>&1 | grep maximum resident`

the mimalloc allocator (which is enabled by default) seems to be faster but doesnt entirely do as much work as the gpa

### nm (liberevo)

the c api functions are `pub export fn` with `callconv(.c)`, and the header is autogenerated from `src/c/bindings.zig`, so it's always in sync with the exports

```sh
zig build lib
nm zig-out/lib/liberevo.a | rg erevo_
```

## architecture

the pipeline is `source -> lexer -> parser -> expander -> semantic -> compiler -> vm`; `build.zig` mirrors it with modules `vm`, `revo`, `c`, `erevo`, plus the exe and the lsp

### pipeline (`src/lang/pipeline.zig`)

`build()` is the whole front end, step by step

errors at any step surface as `lang.Error` variants (`parse`/`expand`/`semantic`/`lower`), all rendered through `diagnostic.zig`

~ `parse` Lexer + Parser produce the ast with `default_macro_source` merged in

~ `wrapModule` module scope only, a synthetic `@exports` table gets built from `pub` decls

~ `preloadImports` walk the ast and load `import`ed modules (the repl skips this)

~ `expand` macros and pipes desugar, `proc` blocks evaluate at compile time

~ `semantic.analyze` name/type analysis against the vm's known globals, fills the per-node `type_annotations` map, resolves imports including `.d.rv` manifests next to shared libs (paired by filename before extension)

~ `lower` the compiler emits the artifact

### lowering (`src/lang/compiler/`)

one pass over the expanded ast, emitting into a pointer-based ir.
`IrInst` operands are `.inst` pointers (data flow by pointer rather than register number), which is what makes the passes below ok. registers come from a stack (`state.zig`, `pushRegister`/`popRegister`), locals are per-function slots, upvalues resolve recursively by name, and every inst carries `fn_depth`

`type_check` infers operand types to pick specialized opcodes (like `add_int` for `add`), `types.zig` is the type system, `flow.zig` lowers control flow, `values.zig` lowers bindings, assigns, patterns, struct/table literals, etc.
the root compiles as `__main` plus `call` + `halt` (`compileRoot`)

... `finishArtifact` runs four passes, then lowers

~ `fold.foldIr` [constant folding](https://en.wikipedia.org/wiki/Constant_folding)

~ `dce.dceIr` [dead code elimination](https://en.wikipedia.org/wiki/Dead-code_elimination)

~ `peephole.peepholeIr` [peephole optimization](https://en.wikipedia.org/wiki/Peephole_optimization), should handle the leftovers fold/dce miss and misc, supposed to be the biggest pass

~ `promote.promoteLoopCarried` [loop-carried dependencies](https://en.wikipedia.org/wiki/Loop_dependence_analysis)

~ then `lowerInst` packs each `IrInst` into the vm's `Instruction`, the artifact (`instructions` + 1:1 `spans`) is copied to the runtime allocator, and nested prototypes get their bytecode segments

### the vm (`src/vm/`)

`module.runCompiledModuleReport` -> `exec.runReport` push the root frame on the main fiber, then loop, run ready fibers, wake sleepers, poll the async backend, wait on io/timers if nothing is runnable. `scheduler.zig` owns the fiber ready queue, channels, and waiters; `VM.Fiber` holds the register file + frames with `top_base` cached for the dispatcher. fibers here are just the cooperative/stackful coroutine model, user code yields control explicitly rather than being preempted

~ `memory.zig` - nanboxed `Data`: raw f64, or `BOX_TAG` + 4-bit type + pool index

~ `pool.zig` + `gc.zig` - append-only object pools with free-list reuse, mark-sweep with finalizers, collection triggers at the smaller of an 8mb nursery budget and the adaptive `gc_threshold` (live bytes * `gc_pause_factor`). the name is a lie (for now), it has no generational split

~ `interner.zig` - strings/atoms to ids; `lookup.zig` - field/method resolution through metatables, with inline caches (2-way table `ICacheEntry`, per-(struct type, atom) `StructCacheEntry`).
the `2-way` here is close-ish to the the one described in [the Holzle/Chambers/Ungar paper](https://bibliography.selflanguage.org/_static/pics.pdf)

~ `bytecode.zig` - on-disk `.rvo` format. always little-endian; `debug.zig` - disassembly + `EvalFailure` reports

~ concurrency: cooperative fibers (with `spawn`/`join`, `chan`/`send`/`recv`), blocking syscalls run on the async backend - `async_backend_posix.zig` (worker threads + completion pipe), `async_backend_none.zig` on windows/freestanding (any no-libc build)

## stdlib (`src/std/`)

to make a new module:
~ copy any existing library - both its `foo.zig` implementation and the `iface/foo.d.rv` declaration\
~ add it into `iface_groups` and `impl_groups` in `api.zig` (i know, it's not the prettiest)

how it works:\
the surface is at `iface/*.d.rv`, files carry doc-comment+declatarion sigs; `api.zig` merges them with the zig `impls` at boot (`register_stdlib`) into `full_specs`, keyed on the bare name, so the doc set always matches the runtime. the primitive type metatable _is_ the module table, so `x:method()` dispatch is a single lookup

## c api, lsp, wasm, repl

- `src/c/` - `erevo.zig` exports embedding api over opaque handles, `bindings.zig` autogenerates `revo.h`, `tests.c` is the c suite (`zig build test-c`)
- `src/lsp/` - revo lsp thru lsp-kit dep, uses `Workspace` together with the front-end, bundled into the `revo` binary (`revo lsp`), stubbed when the `lsp` feature is off
- wasm - `main_wasm.zig` on freestanding targets, io stubbed to panic (since no io on wasm); the dispatch frame can exceed the 1mb default stack so build sets 16mb to fix OOBs
- `src/repl.zig` - uses isocline by default; `-Dfeatures=lsp` or `-Dfeatures=` gives the dumb repl

## build system

`build.zig` has to remain such that zig and git are the only build-time dependencies

the build should also be as static as it can be for its target, unless you're bundling something huge and common

defines the core and optional modules and the features, passed as `-Dfeatures=foo,bar,baz` (`-Dfeatures=` for none).
freestanding gets no io deps.
`zig_backend` opt disables llvm/lld. on linux the default abi is musl, `-Dglibc` switches to llvm + glibc

version: debug builds get the git short hash, release builds `VERSION` from `build.zig`

notable steps

- `zig build run` - run cli from source (`zig build run -- dis file.rv` to pass args)
- `zig build lib` - build `erevo` c library + autogenerated `revo.h` header
- `zig build test -Dtest_filter=<pat>` only tests matching the pattern
- `zig build test-c` - the c api tests (not part of `zig build test`)
- `zig build release` - build release binaries for all targets in `release_targets`

`src/lang/tests.zig` are the integration tests

## docs

the docs for the project itself can be linted and auto-fixed with `rumdl fmt`

docs can be generated via `revo doc` (markdown) or `revo doc --html` (html)

it will normally spit out the output directly, but `--splice` can paste into a template, replacing all that is
between `<!-- docgen:start -->` and `<!-- docgen:end -->`

it will also generate documentation for a whole directory of scripts if pointed at one

this is a fish script i use for regenerating docs

```fish
set STDOCPATH ~/projects/web/revo.lung.fyi/content/std.html
revo doc --html --splice ./src/std/iface < "$STDOCPATH" > ./std.html
mv ./std.html $STDOCPATH
```
